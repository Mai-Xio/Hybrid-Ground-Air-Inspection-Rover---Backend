// Create a copy named wifi_config.h in the same Arduino sketch folder as firmware-wifi.ino.
// Replace the values with your WiFi hotspot/router name and password.
// Keep the quotes.

#pragma once

#define WIFI_SSID_1 "YOUR_WIFI_NAME"
#define WIFI_PASSWORD_1 "YOUR_WIFI_PASSWORD"

#define WIFI_SSID_2 "YOUR_BACKUP_WIFI_NAME"
#define WIFI_PASSWORD_2 "YOUR_BACKUP_WIFI_PASSWORD"
